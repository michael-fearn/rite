Status: ready-for-agent
Type: enhancement

## Parent

.scratch/initial-building-blocks/issues/05-tofu-yaml-to-resource-bridge-vm-lifecycle.md

## What to build

Add the supported OpenTofu wrapper entry point. The wrapper decrypts current Host PVE API token values from Host Sibling SOPS Files into ephemeral `TF_VAR_pve_token_<host>` environment variables, runs tofu from the `tofu/` working directory, and cleans up after itself. Tofu must never read SOPS directly.

## Acceptance criteria

- [ ] A script such as `scripts/tofu-wrap` is the supported tofu entry point.
- [ ] The wrapper reads Host declarations from `inventory/hosts/*.yaml`.
- [ ] For each Host, the wrapper decrypts only `pve_tokens.tofu.value` from `inventory/hosts/<host>.sops.yaml`.
- [ ] The wrapper exports only the matching `TF_VAR_pve_token_<host>` environment variable for each Host token.
- [ ] The wrapper invokes `tofu` with its working directory set to `tofu/`.
- [ ] The wrapper runs provider generation before tofu commands that require generated HCL.
- [ ] Missing Host Sibling SOPS Files or missing tofu token values fail clearly before invoking tofu.
- [ ] Token values are not printed to stdout/stderr by the wrapper.
- [ ] Temporary files or env state are cleaned up on exit.
- [ ] Tests use fake `sops` and fake `tofu` to verify decrypt extracts, env var names, working directory, command forwarding, and cleanup behavior.
- [ ] Tests verify the wrapper does not decrypt entire SOPS files when an extract path is sufficient.
- [ ] Documentation states that direct tofu invocation is unsupported because tofu must never read SOPS.

## Blocked by

.scratch/initial-building-blocks/issues/05.1-tofu-provider-generation-and-vm-partition-scaffold.md

## Design decisions

- Secret transport is split from real VM resource creation.
- PVE tokens are ephemeral process environment only.
- Sensitive variables are declared by the generated/root tofu configuration; the wrapper owns only decryption and invocation.

## Out of scope

- PVE token creation or rotation.
- Host Configure changes.
- Real Proxmox VM resources.
- VM lifecycle orchestration.
