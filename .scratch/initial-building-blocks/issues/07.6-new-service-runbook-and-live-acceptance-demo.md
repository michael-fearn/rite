Status: ready-for-human

## Parent

.scratch/initial-building-blocks/issues/07-quadlet-renderer-first-multi-container-service.md

## What to build

Document the Service authoring and deployment workflow and prove the Quadlet substrate with a live, real-world-shaped multi-container Service acceptance demo on a VM.

## Acceptance criteria

- [x] `runbooks/new-service.md` explains the Service yaml shape, Backend and hostname naming planes, Ingress defaults, Published Ports, Service Groups, Container Aliases, Service-owned volumes, Share-backed Volumes, Service Secrets, Quadlet Fragments, and Service Data Owner
- [x] Runbook explains that Share-backed Volumes reference a Mount Name already declared on the Backend VM; Service yaml does not declare NAS Endpoint, Dataset, Share, or protocol details directly
- [x] Runbook explains that `service-deploy` may validate Share-backed Volume subpaths but does not run NAS Reconcile, create NAS Shares, or create VM Mount units
- [x] Runbook explains that VM placement is the Service security boundary and issue-07 Quadlets are rootful system units
- [x] Runbook warns that Service Data Directory cleanup/migration is explicit; `service-deploy` never prunes `/srv/services/<service>/`
- [x] Runbook notes that Service deletion/destruction is not automated in issue 07
- [ ] Live acceptance demo deploys a contrived but real-world-shaped multi-container Service on a VM
- [x] Demo includes web + postgres-like + redis-like containers, a Service Secret, a Service-owned volume, a Share-backed Volume referencing an existing Backend VM Mount with `source: /`, `depends_on`, multiple Published Ports with one Ingress Backend, optional Service Data Owner, and one validated Quadlet Fragment
- [ ] Demo verifies the Service is reachable through its Ingress hostname when enabled and that same-network container communication works through Container Aliases
- [x] Demo records operator-facing verification commands and expected success/failure signals

## Blocked by

.scratch/initial-building-blocks/issues/07.5-service-deploy-workflow.md

## Comments

- 2026-05-10: Added `runbooks/new-service.md` and `tests/test_new_service_runbook.py`. The runbook documents the Service yaml shape, Backend and hostname naming planes, Ingress defaults, Published Ports, Service Groups, Container Aliases, Service-owned volumes, Share-backed Volumes, Service Secrets, Quadlet Fragments, Service Data Owner, Share-backed Volume ownership boundaries, service-deploy boundaries, rootful VM security boundary, explicit Service Data Directory cleanup/migration, and the lack of automated Service deletion/destruction in issue 07.
- 2026-05-10: Added a renderable live-demo declaration at `inventory/acceptance/service-demo.yaml`, a SOPS example at `inventory/acceptance/service-demo.sops.yaml.example`, and a validated Quadlet Fragment at `inventory/acceptance/services/fortress-service-demo.quadlet.d/web.container`. The fixture covers web + postgres-like + redis-like containers, a Service Secret, a Service-owned volume, a Share-backed Volume with `source: /`, `depends_on`, multiple Published Ports with one Ingress Backend, optional Service Data Owner, Container Aliases, and a Quadlet Fragment.
- 2026-05-10: Automated verification passed with `python3 -m unittest discover -s tests` (`Ran 254 tests in 49.617s`, `OK`). The remaining acceptance step is the gated live VM deployment and Ingress/container-network verification, which requires real Host access and operator approval.
