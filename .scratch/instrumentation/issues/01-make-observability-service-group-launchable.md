# Make observability Service Group launchable

Status: done

## What to build

Make the existing `observability` Service Group launchable from the Observability VM using the established Service Group Launch model. The completed slice should let the inventory graph resolve `service-group-launch observability` as a valid launch intent for the `observability` Service on `observability-vm`.

## Acceptance criteria

- [x] `observability-vm` declares the `observability` Service Group as launchable with an explicit launch order.
- [x] Inventory validation accepts the declaration and rejects stale or inconsistent launch-order wiring in tests.
- [x] The Service Group Launch intent for `observability` resolves to `observability-vm`, the `observability` Service, and the correct ingress regeneration requirement.
- [x] Existing Service Group Launch workflow tests still pass.

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*

Completed. Evidence:

- `observability-vm` declares `launchable_service_groups` for `observability` with launch order `observability`.
- `service_group_launch_intent("observability")` resolves to Backend VM `observability-vm`, Service `observability`, and `requires_ingress_regeneration=True`.
- `build_service_group_launch_plan(..., "observability")` produces `vm-up observability-vm`, `service-deploy observability`, then `ingress-regenerate`.
- Verification passed with `python3 -m fortress_inventory.validate_inventory` and the Service Group Launch-focused unittest selection.
