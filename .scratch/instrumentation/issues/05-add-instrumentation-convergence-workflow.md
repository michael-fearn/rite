# Add Instrumentation Convergence workflow

Status: done

## What to build

Add an explicit Instrumentation Convergence workflow that applies enabled VM-level Instrumentation across ordinary VMs and refreshes the Observability Service from the current Instrumentation declarations. This workflow is the operator path for applying Instrumentation to existing VMs and Services.

## Acceptance criteria

- [x] A script and Just recipe expose `instrumentation-converge`.
- [x] The workflow runs VM Configure for every ordinary VM with VM-level Instrumentation enabled.
- [x] The workflow refreshes the Observability Service using Service Update semantics.
- [x] Service Update supports non-interactive use when called by Instrumentation Convergence.
- [x] Workflow tests cover ordering, opt-out VMs, failure stopping/reporting, and the observability refresh phase.

## Blocked by

- .scratch/instrumentation/issues/03-install-baseline-vm-collectors-through-vm-configure.md
- .scratch/instrumentation/issues/04-generate-observability-service-configuration-from-instrumentation.md

## Completion Notes

> *This was generated by AI during triage.*

Implemented `instrumentation-converge` as an operator workflow script and Just recipe. The workflow builds an ordered plan from current Inventory, runs `vm-configure` for every ordinary VM with VM-level Instrumentation enabled, then refreshes the `observability` Service through `service-update observability --auto-confirm`.

Evidence:

- `python3 -m unittest tests.test_instrumentation_convergence_workflow`
- `python3 -m unittest tests.test_service_update_workflow tests.test_instrumentation_convergence_workflow`
- `python3 -m unittest`
