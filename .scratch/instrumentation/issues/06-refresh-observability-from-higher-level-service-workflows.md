# Refresh observability from higher-level Service workflows

Status: done

## What to build

Update Service Launch and Service Group Launch so higher-level workflows refresh the Observability Service when launched Services declare Instrumentation. Keep Service Deploy scoped to the named Service and do not make it implicitly refresh observability.

## Acceptance criteria

- [x] Service Deploy remains scoped to deploying only the named Service.
- [x] Service Launch refreshes observability when the launched Service declares application-specific Instrumentation.
- [x] Service Group Launch refreshes observability when any launched group Service declares application-specific Instrumentation.
- [x] Observability refresh uses Service Update semantics for the Observability Service.
- [x] Workflow tests cover both refresh and no-refresh cases.

## Blocked by

- .scratch/instrumentation/issues/04-generate-observability-service-configuration-from-instrumentation.md

## Completion Comment

> *This was generated by AI during triage.*

Implemented with vertical TDD cycles against public workflow behavior:

- Added Service Launch plan and script behavior coverage for instrumented Services refreshing observability through `scripts/service-update observability --auto-confirm`.
- Added Service Group Launch plan and script behavior coverage for a single observability refresh when any launched Service declares telemetry-target Instrumentation.
- Added a Service Deploy boundary regression proving an instrumented Service deploy remains scoped to the named Service and does not invoke observability refresh behavior.
- Existing no-refresh workflow cases remain covered by the Service Launch and Service Group Launch tests that assert exact command sequences without `service-update`.

Evidence:

- `python3 -m unittest tests.test_service_launch_workflow tests.test_service_group_launch_workflow tests.test_service_deploy_workflow`
- `./scripts/run-tests-parallel`
