# Expose Observability View intent from Inventory

Status: done

## What to build

Expose generated Observability View intent from Inventory so downstream Observability Service generation can ask which baseline VM views and explicit Service views should exist without traversing raw YAML. The intent should use stable view identities derived from observed Entity identity and view kind.

## Acceptance criteria

- [x] Inventory queries expose baseline VM Observability View intent for VMs with enabled VM-level Instrumentation.
- [x] Inventory queries expose Service Observability View intent for Services that request a valid application-specific Observability View Profile.
- [x] Generated view identity is derived from Entity identity and view kind, not display title.
- [x] View intent excludes VMs omitted from the current Observability generation context when that context excludes absent or skipped VMs.
- [x] Focused tests prove VM baseline view intent, explicit Service view intent, stable IDs, and exclusion behavior.

## Blocked by

- .scratch/instrumentation/issues/09-add-service-observability-view-inventory-contract.md

## Triage Notes

> *This was generated by AI during triage.*
>
> Completed implementation for issue 10. Added `ObservabilityViewIntent` plus public Inventory graph queries for combined, VM baseline, and Service profile Observability View intent. VM baseline intent is emitted for instrumented ordinary VMs, explicit Service intent is emitted from `instrumentation.observability_views`, view IDs derive from entity identity plus view kind/profile, and excluded VM contexts suppress both VM baseline views and Service views resident on excluded backend VMs.
>
> Evidence:
> - `python3 -m unittest tests.test_inventory_entity_graph`
> - `python3 -m unittest tests.test_inventory_cross_file_validator tests.test_inventory_schemas`
