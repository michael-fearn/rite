# Generate prometheus_generic Service Observability View

Status: done

## What to build

Generate one application-specific Service Observability View for each Service that explicitly requests the `prometheus_generic` Observability View Profile and has compatible Prometheus metrics Instrumentation. The generated view should be Service-scoped, not Telemetry Target-scoped.

## Acceptance criteria

- [x] A Service that requests `prometheus_generic` gets exactly one generated application-specific Observability View.
- [x] The generated view can include one or more compatible Prometheus metrics Telemetry Targets for that Service.
- [x] A Service without an explicit application-specific Observability View request does not get a Service-specific generated dashboard.
- [x] Generated Service view identity is stable and derived from the Service identity and view kind.
- [x] Focused tests prove one-view-per-Service behavior, multi-target Service handling, no implicit Service dashboard, and stable identity.

## Blocked by

- .scratch/instrumentation/issues/11-generate-grafana-provisioning-for-observability-views.md

## Triage Completion

> *This was generated by AI during triage.*
> Completed issue 13. Evidence: `python3 -m unittest tests.test_observability_config_generation`, `python3 -m unittest tests.test_inventory_entity_graph`, and `python3 -m unittest tests.test_inventory_cross_file_validator tests.test_inventory_schemas` all pass. The generated Observability Service Grafana artifacts now render one `prometheus_generic` Service dashboard per explicit Service view request, include all compatible Prometheus metrics targets for that Service, avoid implicit Service dashboards, and keep dashboard file/UID identity derived from the Service identity plus profile.
