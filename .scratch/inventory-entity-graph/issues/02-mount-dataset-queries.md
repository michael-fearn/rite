> *This was generated by AI during architecture grilling.*

Status: done
Category: enhancement

# Inventory Entity Graph Mount and Dataset queries

## What to build

Extend the Inventory Entity Graph with read-only Mount and Dataset queries that concentrate cross-Entity knowledge currently repeated by NAS Reconcile, Service Deploy, Quadlet rendering, and Inventory validation.

The Module should answer questions about VM Mounts, the Dataset behind a VM Mount, the VM client addresses used for Derived Share planning, and Service Share-backed Volumes without making callers know the nested raw YAML shape for VMs, Datasets, Services, or Backend placement.

## Acceptance criteria

- [ ] Extend the Inventory Entity Graph rather than creating a separate competing query Module.
- [ ] Add graph queries for VM Mount lookup by VM name and Mount name.
- [ ] Add graph queries that resolve a VM Mount to its Dataset facts, including Dataset name, NAS Endpoint name, path, lifecycle, and owner facts where declared.
- [ ] Add graph queries for VM NFS client addresses using the networking/query basics from issue 01.
- [ ] Add graph queries that expose the facts needed to derive Desired NFS Shares without callers walking VM Mounts and Datasets directly.
- [ ] Add graph queries that resolve a Service's Share-backed Volumes through the Service Backend VM's Mount declarations.
- [ ] Ordinary absence returns `None` or an empty tuple where absence is valid; invalid references or ambiguous declared facts raise the domain-specific Inventory Entity Graph error.
- [ ] Migrate at least one production caller that currently repeats Mount/Dataset traversal to use the graph.
- [ ] Tests cover Mount lookup, Dataset resolution, Derived Share inputs, Service Share-backed Volume resolution, missing references, and invalid address facts.
- [ ] NAS Reconcile and Service Deploy behavior remains compatible from the operator's point of view.

## Blocked by

- `.scratch/inventory-entity-graph/issues/01-networking-query-basics.md`

## Notes

Keep this slice focused on read-only facts. It should not move NAS apply behavior, Service Deploy planning, or Quadlet rendering behind this Module.

## Comments

> *This was generated by AI during triage.*

Implemented and verified. The Inventory Entity Graph now exposes Mount, Dataset, Desired NFS Share input, VM NFS client address, and Service Share-backed Volume facts; NAS Reconcile derives desired NFS Shares through the graph. Focused verification passed with `python3 -m unittest tests.test_inventory_entity_graph tests.test_nas_reconcile_plan`.
