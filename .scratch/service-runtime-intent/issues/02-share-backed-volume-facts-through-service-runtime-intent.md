> *This was generated by AI during triage.*

# Share-backed Volume facts through Service Runtime Intent

Status: ready-for-agent
Category: enhancement
Date: 2026-05-19

## Parent

`.scratch/service-runtime-intent/PRD.md`

## What to build

Move Share-backed Volume runtime meaning into Service Runtime Intent. Fleet-wide analysis should expose resolved Share-backed Volume facts for each Quadlet Service container volume that references a Backend VM Mount, while returning diagnostics for missing Mount Names, unsafe source paths, and access widening.

Inventory validation should adapt those diagnostics into the existing operator-facing validation errors. Service Deploy and Quadlet rendering should consume the resolved facts where they currently rebuild Share-backed Volume source paths, effective access, and required mount units, while preserving the current Ansible variables and rendered Quadlet output.

Service Runtime Intent should own fortress runtime meaning only. It should not render Quadlet files, produce deploy variable names, run Ansible, or inspect decrypted secret values.

## Acceptance criteria

- [ ] Service Runtime Intent exposes Share-backed Volume facts identifying Service, Backend VM, container, volume index, Mount Name, Dataset when available, VM Mount path, resolved source path, container path, effective access mode, and required systemd mount unit.
- [ ] Service Runtime Intent diagnoses missing Share-backed Volume Mount Names before deployment and can return partial facts alongside that diagnostic.
- [ ] Service Runtime Intent diagnoses Share-backed Volume sources that are neither `/` nor safe relative subpaths without `..` traversal.
- [ ] Service Runtime Intent diagnoses Share-backed Volumes that would widen a read-only VM Mount to read-write.
- [ ] Inventory validation adapts the new diagnostics while preserving the current validation codes and operator-facing messages wherever practical.
- [ ] Service Deploy still produces the same `fortress_share_backed_volume_subpaths` values from the resolved runtime facts.
- [ ] Quadlet rendering still produces unchanged `Volume=`, `Requires=`, and `After=` lines for Share-backed Volumes, including systemd-escaped mount units.
- [ ] Direct Service Runtime Intent tests cover root Mount binding, relative subpath binding, resolved source paths, effective access, required mount units, missing Mount diagnostics, unsafe source diagnostics, and access-widening diagnostics.

## Blocked by

None - can start immediately
