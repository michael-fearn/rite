> *This was generated by AI during triage.*

# Service Data Directory facts through Service Runtime Intent

Status: done
Category: enhancement
Date: 2026-05-19

## Parent

`.scratch/service-runtime-intent/PRD.md`

## What to build

Move Service-owned volume and Service Data Directory runtime meaning into Service Runtime Intent. Fleet-wide analysis should expose facts for Service-owned bind mounts under `/srv/services/<service>/`, including the declared Service Path, resolved VM-local path, container path, effective access mode, and Service Data Owner when declared.

Service Deploy and Quadlet rendering should consume these facts where they currently discover Service Data Directories from raw Service yaml. Preserve current behavior: Service Data Directory contents remain unpruned, Service Data Owner applies only to Service-owned paths, and generated Service Data Files remain Adapter-owned.

## Acceptance criteria

- [x] Service Runtime Intent exposes Service-owned volume facts identifying Service, container, volume index, Service Path, resolved VM-local path, container path, effective access mode, and Service Data Owner when declared.
- [x] Service Runtime Intent exposes de-duplicated Service Data Directory facts derived from Service-owned volume facts.
- [x] Service Data Owner is applied only to Service Data Directory facts and not to Share-backed Volume facts.
- [x] Service Deploy still creates the same `fortress_service_data_directories` entries for Service-owned volumes.
- [x] Service Deploy does not prune Service Data Directory contents as part of this migration.
- [x] Quadlet rendering still produces unchanged `Volume=` lines for Service-owned volumes.
- [x] Observability generated Service Data Files and Application Configuration Artifacts remain Adapter-owned and are not moved into Service Runtime Intent.
- [x] Direct Service Runtime Intent tests cover resolved Service Data Directory paths, duplicate directory de-duplication, effective access modes, and Service Data Owner application.

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*
>
> Implemented Service-owned volume and de-duplicated Service Data Directory facts in Service Runtime Intent, including resolved `/srv/services/<service>/...` VM paths, container paths, effective `ro`/`rw` access modes, and declared Service Data Owner. Service Deploy now consumes the runtime-intent directory facts for `fortress_service_data_directories` when the fleet model is available, while Quadlet rendering keeps existing `Volume=` output and compatibility fallback behavior. Generated Service Data Files, observability artifacts, and Application Configuration Artifacts remain adapter-owned; no Service Data Directory pruning behavior was added.
>
> Verification: `python3 -m unittest tests.test_service_runtime_intent tests.test_service_quadlet_rendering tests.test_service_deploy_workflow` passed, 65 tests. `python3 -m unittest discover tests` passed, 623 tests.
