> *This was generated by AI during triage.*

# Quadlet Service Secret facts through Service Runtime Intent

Status: done
Category: enhancement
Date: 2026-05-19

## Parent

`.scratch/service-runtime-intent/PRD.md`

## What to build

Move Quadlet Service Secret reference and wiring meaning into Service Runtime Intent. Fleet-wide analysis should expose Service Secret facts for Quadlet containers, including the secret key, service-scoped Podman secret name, environment variable name, SOPS extraction path, and whether the container receives a secret file path or the Podman secret name.

Inventory validation should adapt Service Runtime Intent diagnostics for invalid Service Secret references and invalid environment wiring. Service Deploy should use the facts to build the same secret installation vars and preflight required keys. Quadlet rendering should use the facts for `Secret=` and `Environment=` lines while preserving rendered output.

Service Runtime Intent must not decrypt SOPS, hold plaintext secret values, or become a deploy-var renderer.

## Acceptance criteria

- [x] Service Runtime Intent exposes Quadlet Service Secret facts identifying Service, container, secret index, secret key, service-scoped Podman secret name, environment variable name, SOPS extraction path, and env value mode.
- [x] Service Runtime Intent diagnoses Service Secret references that do not use `secrets.<name>`.
- [x] Service Runtime Intent diagnoses Service Secret file-path wiring whose environment variable does not end in `_FILE`.
- [x] `env_value: secret_name` remains supported and is represented as an explicit runtime fact.
- [x] Inventory validation adapts Service Secret diagnostics while preserving current validation codes and operator-facing messages wherever practical.
- [x] Service Deploy still produces the same Service Secret installation variables and preflight required secret keys.
- [x] Quadlet rendering still produces unchanged Podman `Secret=` lines and Service Secret environment wiring.
- [x] Direct Service Runtime Intent tests cover service-scoped Podman names, `secrets.<name>` extraction paths, default `_FILE` path wiring, `env_value: secret_name`, invalid references, invalid env wiring, and partial facts with diagnostics.

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*
> Implemented Quadlet Service Secret runtime facts in Service Runtime Intent, including service/container identity, secret indexes, service-scoped Podman names, SOPS extraction paths, and explicit env value mode. Runtime Intent now diagnoses invalid Service Secret references and invalid file-path environment wiring while preserving partial facts. Inventory validation adapts those diagnostics with existing codes/messages, Service Deploy builds Service Secret install vars and preflight keys from runtime facts, and Quadlet rendering consumes the same facts for `Secret=` and `Environment=` output without decrypting SOPS or holding plaintext. Verified with targeted Runtime Intent, validation, deploy, and rendering tests plus full unittest discovery.
