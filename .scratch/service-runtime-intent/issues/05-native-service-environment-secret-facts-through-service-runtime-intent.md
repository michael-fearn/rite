> *This was generated by AI during triage.*

# Native Service Environment Secret facts through Service Runtime Intent

Status: done
Category: enhancement
Date: 2026-05-19

## Parent

`.scratch/service-runtime-intent/PRD.md`

## What to build

Move Native Service Environment Secret reference meaning into Service Runtime Intent while keeping it distinct from Quadlet Service Secret installation. Fleet-wide analysis should expose Native Service Environment Secret facts for Native Services, including the secret key, environment variable name, and structured SOPS extraction path.

Inventory validation should adapt Service Runtime Intent diagnostics for invalid Native Service Environment Secret references. Service Deploy should use the facts to build the same native environment secret specs and preflight required keys. Native config/template deployment remains Adapter-owned.

Service Runtime Intent must not decrypt SOPS, hold plaintext secret values, install Podman secrets for Native Services, or render native config files.

## Acceptance criteria

- [x] Service Runtime Intent exposes Native Service Environment Secret facts identifying Service, secret index, secret key, environment variable name, and SOPS extraction path.
- [x] Service Runtime Intent diagnoses Native Service Environment Secret references that do not use `secrets.<name>`.
- [x] Native Service Environment Secret facts use the same structured SOPS entry interpretation as Quadlet Service Secrets.
- [x] Native Service Environment Secret facts remain distinct from Quadlet Service Secret facts and do not imply Podman secret installation.
- [x] Inventory validation adapts Native Service Environment Secret diagnostics while preserving current validation codes and operator-facing messages wherever practical.
- [x] Service Deploy still produces the same `fortress_native_environment_secret_specs` values and preflight required secret keys.
- [x] Direct Service Runtime Intent tests cover environment variable names, SOPS extraction paths, invalid references, and partial facts with diagnostics.

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*
>
> Implemented Native Service Environment Secret runtime facts on Service Runtime Intent, including Service name, secret index, secret key, environment variable name, and structured `["secrets"][...]["value"]` SOPS extraction path. Invalid Native Service Environment Secret references now emit runtime diagnostics that inventory validation adapts with the existing validation code/message, and Service Deploy consumes runtime facts for native environment secret specs and preflight keys without installing Podman secrets for Native Services. Verified with focused runtime intent, validation, service deploy workflow tests, and the full unittest suite.
