> *This was generated by AI during triage.*

# Per-Service runtime intent view and import migration guardrails

Status: done
Category: enhancement
Date: 2026-05-19

## Parent

`.scratch/service-runtime-intent/PRD.md`

## What to build

Introduce or finish a per-Service Service Runtime Intent convenience view that is derived from the canonical fleet-wide analysis result. The view should make single-Service deploy and rendering code easier to read without creating a second analysis path or different semantics.

Keep the compatibility Adapter at the old `fortress_services.runtime_intent` import location working while new code prefers the canonical Inventory-owned Service Runtime Intent module. Tests should make this boundary hard to accidentally regress.

## Acceptance criteria

- [x] The canonical fleet-wide Service Runtime Intent analysis remains the only source of runtime facts and diagnostics.
- [x] A per-Service convenience view, if added, is derived from the fleet-wide result rather than recomputing from raw Service yaml.
- [x] Single-Service callers can access facts for one Service without receiving facts for unrelated Services.
- [x] Tests prove the per-Service view returns the same facts and diagnostics as filtering the fleet-wide result.
- [x] The `fortress_services.runtime_intent` compatibility Adapter continues to work.
- [x] New or migrated code imports the canonical Inventory-owned Service Runtime Intent module where practical.
- [x] Runtime terminology in tests and issue-facing names consistently uses Service Runtime Intent, Share-backed Volume, Service Data Directory, Service Secret, and Native Service Environment Secret.

## Blocked by

- `.scratch/service-runtime-intent/issues/02-share-backed-volume-facts-through-service-runtime-intent.md`
- `.scratch/service-runtime-intent/issues/03-service-data-directory-facts-through-service-runtime-intent.md`
- `.scratch/service-runtime-intent/issues/04-quadlet-service-secret-facts-through-service-runtime-intent.md`
- `.scratch/service-runtime-intent/issues/05-native-service-environment-secret-facts-through-service-runtime-intent.md`

## Comments

> *This was generated by AI during triage.*

Implemented a per-Service Service Runtime Intent view derived only from the canonical fleet-wide analysis result, and re-exported it through the `fortress_services.runtime_intent` compatibility Adapter. Single-Service deploy and Quadlet rendering paths now prefer the canonical Inventory-owned Service Runtime Intent view where a runtime result is available, including Service Secret rendering and deploy preflight/install facts. Added regression tests proving the per-Service view matches filtering the fleet-wide result, the compatibility Adapter remains usable, and Quadlet Service Secret rendering consumes Service Runtime Intent facts instead of recomputing from raw Service yaml. Verified with focused runtime/deploy/rendering/validation tests and full unittest discovery.
