name: immich
hostname: photos.fearn.cloud
backend:
  vm: media01
  port: 2283
ingress:
  enabled: true
deploy:
  type: quadlet
  containers:
    - name: server
      image: ghcr.io/immich-app/immich-server:v1.120.0
      published_ports:
        - container: 2283
          ingress: true
